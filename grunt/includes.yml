options:
  filenameSuffix: .js
  duplicates: false
  debug: <%= production %>


build-standalone:
  files:
    - src:  <%= path.src %>
      dest: <%= path.tmp.standalone %>

build-jquery:
  files:
    - src:  <%= path.src %>
      dest: <%= path.tmp.jquery %>


